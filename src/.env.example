# Optional, for local development only: pre-fill the Groq API key so you
# don't have to paste it into the UI. Do NOT set this for production builds —
# env vars are baked into the public bundle. Users paste their own key in the
# Content tab (stored in localStorage on their device).
VITE_GROQ_API_KEY=your_groq_api_key_here
